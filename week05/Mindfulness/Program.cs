using System;
using Mindfulness;

class Program
{
    static void Main(string[] args)
    {
        // I added two things
        // 1. the breathing activity has an animation of the text slowly appearing to mimic the breathing speed.
        // 2. The reflecting activity will not select the same question twice in the same session.
        ConsoleInterface.RunInterface();
    }
}